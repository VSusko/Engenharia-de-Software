//#include "model_impl.h"


int main()
{
    /*
    ModelFactory &f1 = ModelFactory::getInstance();

    System *system1 = f1.createSystem("s1", 10);
    System *system2 = f1.createSystem("s2", 20);
    Flow *flow = f1.createFlow("flow1", system1, system2);
    Model *model = ModelFactory::createModel("m1");

    cout << "System 1: " << system1->getName() << " (" << system1->getValue() << ")\n";
    cout << "System 2: " << system2->getName() << " (" << system2->getValue() << ")\n";
    cout << "Flow: " << flow->getName() << " conecta " 
              << flow->getSource()->getName() << " -> " 
              << flow->getTarget()->getName() << "\n";
    cout << "Model: " << model->getName() << "\n";

    delete system1;
    delete system2;
    delete flow;
    delete model;
    */
    return 0;
}